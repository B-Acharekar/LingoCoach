<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.LingoCoach" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Branded window shown immediately while Compose is starting. -->
        <item name="android:windowBackground">@drawable/splash_window_background</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:statusBarColor">#6A5CFF</item>
        <item name="android:navigationBarColor">#6A5CFF</item>
    </style>

</resources>
